{
  "task": "Implement incremental pipeline-run persistence in composer, per .pi/loop-guide.md (read it first, it is the binding spec). Four pieces: (1) UpdateActive + FailInterrupted methods on pipeline.RunRepository, (2) store implementation in internal/infra/store/pipeline_repo.go with the SQL terminal-state guard, leaving the existing Update method untouched, (3) an optional persist hook on PipelineExecutor (setter, not a constructor change) called after run.Start() and after each batch's RecordStepResult loop, guarded by the run context, errors never failing the run, (4) wiring in NewPipelineService plus a FailInterrupted reaper call in cmd/composerd/main.go. Write the three contract tests the guide names exactly: TestRunRepo_UpdateActive, TestRunRepo_FailInterrupted, TestPipelineExecutor_IncrementalPersist. Update any test fakes implementing RunRepository.",
  "maxIterations": 15,
  "models": ["llama-server/loop", "llama-server/qwen3-coder", "anthropic/claude-sonnet-5"],
  "stallPatience": 3,
  "tools": ["read", "edit", "write", "bash", "grep", "glob"],
  "writeScope": [
    "internal/domain/pipeline/**",
    "internal/app/pipeline_*",
    "internal/infra/store/pipeline_repo*",
    "cmd/composerd/main.go"
  ],
  "agentTimeoutMs": 2700000,
  "timeoutMs": 600000,
  "guide": [".pi/loop-guide.md"],
  "rules": [
    "NEVER run ./composerd, go run ./cmd/composerd/, or any built composer binary - its startup hook AES-encrypts ~/.ssh and the keys are unrecoverable. Verification is go test / go build / go vet ONLY.",
    "Do not change the existing RunRepo.Update method's SQL, signature, or semantics - CancelRun depends on them.",
    "Persist errors from the incremental hook must be warn-logged and swallowed - a persistence failure must never fail a pipeline run.",
    "No new module dependencies; stdlib + existing deps only.",
    "Do not modify, rename, or weaken any existing test. New tests use the exact names the guide specifies.",
    "If go commands fail inside the sandbox on a read-only build cache, export GOCACHE=/tmp/gocache GOMODCACHE=$(go env GOMODCACHE).",
    "A sensor satisfied by a stub (panic(\"not implemented\"), TODO body) is a FAILED iteration.",
    "If you need a paragraph-long comment to justify a workaround, the code is wrong - fix the code.",
    "Never stage content in scratch files (new_tests.txt etc) - write code and tests directly into their final files with the edit/write tools.",
    "Work in dependency order and keep the tree compiling: interface first, then store impl, then executor, then wiring, then tests. Before finishing, run: go build ./... && go vet ./... && go test ./internal/infra/store/ ./internal/app/ ./internal/domain/pipeline/ -count=1. An iteration that leaves the tree broken is always rolled back."
  ],
  "sensors": [
    {
      "name": "build",
      "cmd": "go build ./...",
      "hint": "compile error - read the message, fix the code named in it. web/dist is already built; do not touch frontend code.",
      "canary": "printf '\\nfunc zzLoopCanaryBroken() { undefinedSymbol() }\\n' >> internal/app/pipeline_executor.go"
    },
    {
      "name": "vet",
      "cmd": "go vet ./...",
      "hint": "vet type-checks test files too - a test fake missing the new RunRepository methods fails here, not at build.",
      "canary": "printf '\\nfunc zzLoopCanaryVet() { _ = fmt.Sprintf(\"%d\", \"x\") }\\n' >> internal/app/pipeline_executor.go"
    },
    {
      "name": "no-stubs",
      "cmd": "test -z \"$(rg --no-messages 'panic\\(\\\"(TODO|not implemented)' internal/ cmd/)\"",
      "hint": "a stubbed function passes build but gates nothing - implement it for real.",
      "canary": "printf '\\nfunc zzLoopCanaryStub() { panic(\"not implemented\") }\\n' >> internal/app/pipeline_executor.go"
    },
    {
      "name": "test-domain",
      "cmd": "go test ./internal/domain/pipeline/... -count=1",
      "hint": "domain run/pipeline tests must stay green - you may add methods to the interface but not change existing aggregate behavior."
    },
    {
      "name": "test-store",
      "cmd": "go test ./internal/infra/store/ -count=1",
      "hint": "existing store tests must stay green - Update is untouched; new behavior goes in new methods.",
      "canary": "printf '\\nfunc TestZZLoopCanary(t *testing.T) { t.Fatal(\"canary\") }\\n' >> internal/infra/store/pipeline_repo_test.go"
    },
    {
      "name": "test-app",
      "cmd": "go test ./internal/app/ -count=1",
      "timeoutMs": 900000,
      "hint": "app tests fake their deps - a compile break here usually means a fake RunRepository is missing the new interface methods."
    },
    {
      "name": "test-count-floor",
      "cmd": "test $(go test ./internal/app ./internal/infra/store ./internal/domain/pipeline -list '.*' 2>/dev/null | grep -c '^Test') -ge 157",
      "hint": "tests were removed, renamed, or stopped being collected - restore them. The count only goes up."
    },
    {
      "name": "feature-update-active",
      "expect": "fail",
      "cmd": "go test ./internal/infra/store/ -run 'TestRunRepo_UpdateActive' -count=1 -v 2>&1 | grep -q -- '--- PASS: TestRunRepo_UpdateActive'",
      "hint": "write TestRunRepo_UpdateActive per the guide: active run persists, terminal run is a nil no-op. The test must exist AND pass."
    },
    {
      "name": "feature-fail-interrupted",
      "expect": "fail",
      "cmd": "go test ./internal/infra/store/ -run 'TestRunRepo_FailInterrupted' -count=1 -v 2>&1 | grep -q -- '--- PASS: TestRunRepo_FailInterrupted'",
      "hint": "write TestRunRepo_FailInterrupted per the guide: pending+running flip to failed, terminal rows untouched, count returned."
    },
    {
      "name": "feature-executor-persist",
      "expect": "fail",
      "cmd": "go test ./internal/app/ -run 'TestPipelineExecutor_IncrementalPersist' -count=1 -v 2>&1 | grep -q -- '--- PASS: TestPipelineExecutor_IncrementalPersist'",
      "hint": "write TestPipelineExecutor_IncrementalPersist per the guide: a recording fake persister sees a call after start and after each step batch, accumulating."
    }
  ]
}
